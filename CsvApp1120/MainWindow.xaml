<Window x:Class="CsvApp1120.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CsvApp1120"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="700">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button x:Name="btnOpen" Content="Open" HorizontalAlignment="Left" Grid.Row="0"  VerticalAlignment="Top" Width="75" Click="btnOpen_Click" RenderTransformOrigin="0.957,0.514" Margin="0"/>
        <Button x:Name="btnSave" Content="Save" HorizontalAlignment="Left" Grid.Row="0"  VerticalAlignment="Top" Width="75" Click="btnSave_Click" Margin="120,0,0,0"/>
        <DataGrid x:Name="dataGrid1" Grid.Row="1" Margin="0" MinHeight="300" MinWidth="600"/>
    </Grid>
</Window>
